{
  "_id": "ObjectId()",
  "coureId": "ObjectId",
  "title": "string",
  "slug": "string",
  "content": "string",
  "template": "string",
  "createdAt": "string",
  "updatedAt": "string",
  "order": "number",
  "main_function": "string",
  "testConfig": {
    "framework": "string",
    "timeoutMs": "number",
    "memoryLimitMb": "number",
    "mainFunction": "string",
    "testTemplate": "const userSolution = __USER_CODE__;\n\ndescribe('Square Function', () => {\n  test('should handle positive numbers', () => {\n    expect(userSolution(5)).toBe(25);\n    expect(userSolution(10)).toBe(100);\n  });\n\n  test('should handle zero', () => {\n    expect(userSolution(0)).toBe(0);\n  });\n\n  test('should handle negative numbers', () => {\n    expect(userSolution(-4)).toBe(16);\n  });\n});",
    "testCases": [
      {
        "description": "string",
        "input": "string",
        "output": "string"
      }
    ],
    "dependencies": [
      {
        "name": "string",
        "version": "string"
      }
    ]
  },
  "additionalResources": [
    {
      "title": "string",
      "url": "string"
    }
  ],
  "tags": ["string"]
}
