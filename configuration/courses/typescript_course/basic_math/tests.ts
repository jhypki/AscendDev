describe("Lesson 1", () => {
  test("Test 1", () => {
    expect(square(2)).toBe(4);
  });
  test("Test 2", () => {
    expect(square(3)).toBe(9);
  });
  test("Test 3", () => {
    expect(square(4)).toBe(16);
  });
});
